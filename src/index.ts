// @ts-nocheck

import './styles/default.scss';
import './p5'
// import './canvas'

// var frames = 0
// var ts = 0
// var fps = Math.floor(frames / (ts / 1000))
// var bpm = 140
// var bps = bpm / 60
// var bpmFrames = 0
// var bpmDelay = Math.floor((fps * 1) / bps)
// var framesDiv = $('<div></div>').appendTo(document.body)

// requestAnimationFrame(function animate(t) {
//    frames++
//    ts = t
//    fps = Math.floor(frames / (ts / 1000))
//    draw()
//    requestAnimationFrame(animate)
// })

// function draw() {
//    bpmDelay = Math.floor((fps * 1) / bps)
//    framesDiv.html(Math.floor(bpmFrames / (ts / 1000)))
//    // console.log(bpmDelay)
//    if (frames % bpmDelay === 0) {
//       bpmFrames++
//    }
// }
